import { describe, test } from 'vitest'

describe('CityPage', () => {
  test.todo('renders post titles from feed in the page')
  test.todo('renders city name as heading')
  test.todo('shows not found for unknown city slug')
})
