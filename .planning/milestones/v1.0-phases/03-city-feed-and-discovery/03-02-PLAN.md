---
phase: 03-city-feed-and-discovery
plan: "02"
type: tdd
wave: 2
depends_on: ["03-00", "03-01"]
files_modified:
  - src/app/cities/[slug]/page.tsx
  - src/app/cities/[slug]/loading.tsx
  - src/components/feed/FeedCard.tsx
  - src/components/feed/CategoryFilterTabs.tsx
  - src/components/feed/FeedList.tsx
  - tests/feed/city-page.test.tsx
  - tests/feed/category-tabs.test.tsx
autonomous: true
requirements: [FEED-01, FEED-02]

must_haves:
  truths:
    - "Visiting /cities/paris-france renders a public city page with place and event posts in recency order"
    - "The page shows the city name as a heading"
    - "Category filter tabs are present and each tab links to the correct URL (?category=... or clean slug URL for All)"
    - "A 404 is returned for an unknown city slug"
    - "The page is publicly accessible without authentication (no redirect to /login)"
    - "A 'Load more' link appears when more posts exist and is absent on the last page"
  artifacts:
    - path: "src/app/cities/[slug]/page.tsx"
      provides: "Async Server Component city page — reads params.slug and searchParams.category"
      exports: []
    - path: "src/app/cities/[slug]/loading.tsx"
      provides: "Skeleton fallback for Suspense during city page load"
    - path: "src/components/feed/FeedCard.tsx"
      provides: "Card UI for a single FeedPost (place or event)"
    - path: "src/components/feed/CategoryFilterTabs.tsx"
      provides: "Link-based category filter tabs (Server Component)"
    - path: "src/components/feed/FeedList.tsx"
      provides: "List of FeedCards with Load more link"
  key_links:
    - from: "src/app/cities/[slug]/page.tsx"
      to: "src/lib/db/queries/feed.ts"
      via: "imports getFeedForCity"
      pattern: "getFeedForCity"
    - from: "src/app/cities/[slug]/page.tsx"
      to: "src/lib/db/queries/cities.ts"
      via: "imports getCityBySlug"
      pattern: "getCityBySlug"
    - from: "src/app/cities/[slug]/page.tsx"
      to: "src/components/feed/CategoryFilterTabs.tsx"
      via: "renders <CategoryFilterTabs activeCategory citySlug>"
      pattern: "CategoryFilterTabs"
    - from: "src/app/cities/[slug]/page.tsx"
      to: "src/components/feed/FeedList.tsx"
      via: "renders <FeedList posts nextCursor citySlug>"
      pattern: "FeedList"
---

<objective>
Build the city feed page (FEED-01) and category filter tabs (FEED-02). This is the primary tourist-facing surface: a public route at /cities/[slug] showing a recency-first feed of place posts and events, filterable by category tabs.

Purpose: Tourists can discover local posts for a city. Locals can see their city's content.
Output: City page route, feed card component, category filter tabs, feed list with load-more pagination.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-city-feed-and-discovery/03-RESEARCH.md
@.planning/phases/03-city-feed-and-discovery/03-01-SUMMARY.md

# Middleware already has /cities in public paths — no auth change needed.
# Next.js 16 App Router: params and searchParams are Promises — must await both.
# CategoryFilterTabs can be a pure Server Component (Link only, no useRouter/useState).
# FeedList: Server Component rendering FeedCards + a "Load more" Link pointing to
#   /cities/[slug]?category=...&cursor=[nextCursor.id]&cursorAt=[nextCursor.createdAt.toISOString()]
# No infinite scroll in Phase 3 — simple link-based pagination.
# Past event exclusion is handled in the query (getPostsForCity) — UI just renders what it receives.
# Date display: use date-fns formatDistanceToNow for relative timestamps ("3 hours ago").
# Category label map (matches Phase 2 PlaceForm): restaurant→'Restaurant', cafe→'Café', bar→'Bar',
#   activity→'Activity', sport→'Sport', tourist_attraction→'Attraction', shopping→'Shopping', other→'Other'
# ISR: export const revalidate = 60 on the city page (per research recommendation for polling).

<interfaces>
<!-- From 03-01-PLAN.md files_modified — types available after plan 03-01 completes -->
From src/lib/db/queries/feed.ts:
  export type FeedPost = {
    id: string; title: string; contentType: 'place' | 'event';
    category: string | null; body: string | null;
    lat: string | null; lng: string | null;
    authorUsername: string | null; createdAt: Date;
    startsAt: Date | null; endsAt: Date | null; firstImagePath: string | null;
  }
  export type FeedCursor = { id: string; createdAt: Date }
  export async function getFeedForCity(cityId, options): Promise<{ posts: FeedPost[]; nextCursor: FeedCursor | null }>

From src/lib/db/queries/cities.ts:
  export async function getCityBySlug(slug: string): Promise<City | null>
  // City has: { id, slug, name, country, lat, lng, ... }
</interfaces>
</context>

<feature>
  <name>City feed page and category filter UI</name>
  <files>
    src/app/cities/[slug]/page.tsx,
    src/app/cities/[slug]/loading.tsx,
    src/components/feed/FeedCard.tsx,
    src/components/feed/CategoryFilterTabs.tsx,
    src/components/feed/FeedList.tsx,
    tests/feed/city-page.test.tsx,
    tests/feed/category-tabs.test.tsx
  </files>
  <behavior>
    CityPage:
    - Async Server Component at src/app/cities/[slug]/page.tsx
    - Awaits params and searchParams (Next.js 16 Promise props)
    - Calls getCityBySlug(slug) → notFound() if null
    - Calls getFeedForCity(city.id, { category }) → passes posts + nextCursor to FeedList
    - Renders: h1 city name, CategoryFilterTabs, FeedList, Suspense-wrapped CityMapLoader placeholder (map wired in plan 03-04)
    - export const revalidate = 60

    FeedCard:
    - Renders title, contentType badge (Place/Event), category label, body excerpt (first 120 chars), relative timestamp
    - For events: shows startsAt date if present
    - No images in Phase 3 (photos are a Phase 2 detail page feature — feed cards show text only)

    CategoryFilterTabs:
    - Pure Server Component, no 'use client'
    - 9 tabs: All, Restaurants, Cafes, Bars, Activities, Sport, Attractions, Shopping, Events
    - All tab: href=/cities/[slug]
    - Others: href=/cities/[slug]?category=[value]
    - Active tab indicated by aria-current="page" on the active Link

    FeedList:
    - Server Component receiving posts: FeedPost[], nextCursor: FeedCursor | null, citySlug: string, activeCategory?: string
    - Maps posts to FeedCard components
    - If posts is empty: shows "No posts found for this city yet."
    - If nextCursor: shows <Link href="/cities/[slug]?cursor=...&cursorAt=..."> Load more </Link>
  </behavior>
  <implementation>
    TDD: write failing tests first (RTL render with mocked getFeedForCity / getCityBySlug), then implement.
    Use vi.mock for db query functions. RTL render the page or components directly where feasible.
    CategoryFilterTabs and FeedCard are pure components — test without mocks.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — Write failing tests for city page and category tabs</name>
  <files>tests/feed/city-page.test.tsx, tests/feed/category-tabs.test.tsx</files>
  <behavior>
    city-page.test.tsx:
    - Renders city name from mocked getCityBySlug
    - Renders post titles from mocked getFeedForCity
    - Calls notFound() when getCityBySlug returns null (mock next/navigation notFound)

    category-tabs.test.tsx:
    - Renders "All" tab with href /cities/paris-france
    - Renders "Restaurants" tab with href /cities/paris-france?category=restaurant
    - Renders "Events" tab with href /cities/paris-france?category=event
    - "All" tab has aria-current="page" when activeCategory is "all"
    - "Restaurants" tab has aria-current="page" when activeCategory is "restaurant"
  </behavior>
  <action>
Replace test.todo() stubs in tests/feed/city-page.test.tsx and tests/feed/category-tabs.test.tsx with real RTL test implementations. Mock '@/lib/db/queries/feed' and '@/lib/db/queries/cities' using vi.mock. For city-page tests, mock 'next/navigation' to spy on notFound. The component files do not exist yet — tests MUST fail (RED). Commit: test(03-02): add failing tests for city page and category filter tabs
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && npx vitest run tests/feed/city-page.test.tsx tests/feed/category-tabs.test.tsx 2>&1 | grep -E "FAIL|failed|Cannot find" | head -10</automated>
  </verify>
  <done>Tests run and FAIL (RED). Commit created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — Implement city feed page, FeedCard, CategoryFilterTabs, FeedList</name>
  <files>
    src/app/cities/[slug]/page.tsx
    src/app/cities/[slug]/loading.tsx
    src/components/feed/FeedCard.tsx
    src/components/feed/CategoryFilterTabs.tsx
    src/components/feed/FeedList.tsx
  </files>
  <behavior>
    All tests from Task 1 pass. City page renders without runtime error. Category tabs have correct hrefs and aria-current.
  </behavior>
  <action>
Create all five component/page files so the failing tests turn green.

src/components/feed/FeedCard.tsx:
- Props: post: FeedPost
- Server Component (no 'use client')
- Render article element with: title (h2 or h3), badge for contentType ('Place' | 'Event'), category label using CATEGORY_LABELS map matching Phase 2 (restaurant→'Restaurant', cafe→'Café', etc.), body excerpt (body?.slice(0,120) + (body?.length > 120 ? '...' : '')), relative time using date-fns formatDistanceToNow(createdAt, { addSuffix: true }), author username if present, startsAt date for events (format with date-fns format(startsAt, 'PPp'))

src/components/feed/CategoryFilterTabs.tsx:
- Server Component, NO 'use client'
- Props: activeCategory: string, citySlug: string
- Render nav with 9 Links. Use aria-current="page" on the active tab Link.
- CATEGORIES constant:
  [{ value: 'all', label: 'All' }, { value: 'restaurant', label: 'Restaurants' },
   { value: 'cafe', label: 'Cafes' }, { value: 'bar', label: 'Bars' },
   { value: 'activity', label: 'Activities' }, { value: 'sport', label: 'Sport' },
   { value: 'tourist_attraction', label: 'Attractions' }, { value: 'shopping', label: 'Shopping' },
   { value: 'event', label: 'Events' }]
- All tab href: `/cities/${citySlug}`; others: `/cities/${citySlug}?category=${value}`

src/components/feed/FeedList.tsx:
- Server Component
- Props: posts: FeedPost[], nextCursor: FeedCursor | null, citySlug: string, activeCategory?: string
- Maps posts → <FeedCard post={post} key={post.id} />
- Empty state: <p>No posts found for this city yet.</p>
- Load more: if nextCursor, render <Link href={buildNextPageHref(...)}>Load more</Link>
  where buildNextPageHref constructs: /cities/[slug]?cursor=[id]&cursorAt=[ISO]&category=[cat]

src/app/cities/[slug]/loading.tsx:
- Simple skeleton: <div aria-label="Loading city feed">Loading...</div>
- Keep minimal — Tailwind skeleton classes optional

src/app/cities/[slug]/page.tsx:
- export const revalidate = 60
- type Props = { params: Promise<{ slug: string }>; searchParams: Promise<{ category?: string; cursor?: string; cursorAt?: string }> }
- await both props. getCityBySlug(slug) → notFound() if null.
- Parse cursor if present: { id: cursor, createdAt: new Date(cursorAt) }
- getFeedForCity(city.id, { category, cursor: parsedCursor })
- Return JSX: <main>, <h1>{city.name}</h1>, <CategoryFilterTabs activeCategory={category ?? 'all'} citySlug={slug} />, <FeedList posts={posts} nextCursor={nextCursor} citySlug={slug} activeCategory={category} />, <div id="map-placeholder" /> (wired to actual map in plan 03-04)

Commit: feat(03-02): city feed page, FeedCard, CategoryFilterTabs, FeedList
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && npx vitest run tests/feed/city-page.test.tsx tests/feed/category-tabs.test.tsx 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>All city-page and category-tabs tests pass (GREEN). Full suite green. Commit created.</done>
</task>

</tasks>

<verification>
npx vitest run tests/feed/ — all tests pass.
npx vitest run — full suite green.
src/app/cities/[slug]/page.tsx exists with export const revalidate = 60.
src/components/feed/CategoryFilterTabs.tsx has no 'use client' directive.
src/components/feed/FeedCard.tsx has no 'use client' directive.
</verification>

<success_criteria>
City feed page route exists. Category tabs render with correct hrefs and aria-current. FeedCard renders post content. All tests green. Full suite green.
</success_criteria>

<output>
After completion, create .planning/phases/03-city-feed-and-discovery/03-02-SUMMARY.md
</output>
