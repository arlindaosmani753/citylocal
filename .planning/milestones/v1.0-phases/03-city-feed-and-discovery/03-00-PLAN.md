---
phase: 03-city-feed-and-discovery
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/feed/feed-service.test.ts
  - tests/feed/city-page.test.tsx
  - tests/feed/category-tabs.test.tsx
  - tests/cities/city-search.test.ts
  - tests/map/city-map.test.tsx
autonomous: true
requirements: [FEED-01, FEED-02, FEED-03, FEED-04]

must_haves:
  truths:
    - "All five test stub files exist and the full test suite passes (green — no failures)"
    - "Every stub uses test.todo() with zero src imports — no false positives before implementation"
    - "Test directories (tests/feed/, tests/cities/, tests/map/) exist"
  artifacts:
    - path: "tests/feed/feed-service.test.ts"
      provides: "Stubs for FEED-01, FEED-02, FEED-04, and pagination behaviors"
    - path: "tests/feed/city-page.test.tsx"
      provides: "Stub for FEED-01 city page render"
    - path: "tests/feed/category-tabs.test.tsx"
      provides: "Stubs for FEED-02 category filter tab links"
    - path: "tests/cities/city-search.test.ts"
      provides: "Stubs for FEED-03 query functions"
    - path: "tests/map/city-map.test.tsx"
      provides: "Stub for FEED-04 CityMap client component"
  key_links:
    - from: "tests/feed/feed-service.test.ts"
      to: "src/lib/db/queries/feed.ts"
      via: "test.todo() — no import yet"
      pattern: "test\\.todo"
    - from: "tests/map/city-map.test.tsx"
      to: "src/components/map/CityMap.tsx"
      via: "test.todo() with vi.mock('react-leaflet') comment"
      pattern: "test\\.todo"
---

<objective>
Create the Wave 0 test stub files for Phase 3. All stubs use test.todo() with zero src imports so the full suite stays green (zero failures) before any implementation exists.

Purpose: Nyquist compliance — every behavior has a named test before any code is written. Wave 1 and Wave 2 tasks replace these stubs with real tests.
Output: Five test files covering all FEED-01 through FEED-04 behaviors.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-city-feed-and-discovery/03-VALIDATION.md

# Established pattern from prior phases
# ALL stubs use test.todo() — no src imports, no describe blocks importing modules.
# This matches Phase 01 and Phase 02 Wave 0 pattern confirmed in STATE.md decisions.
# Test runner: npx vitest run (Vitest 4.x, config at vitest.config.ts)
# Existing test dirs: tests/auth/, tests/posts/, tests/profile/, tests/roles/, tests/rsvp/
# New dirs needed: tests/feed/, tests/cities/, tests/map/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create feed and city test stub files</name>
  <files>
    tests/feed/feed-service.test.ts
    tests/feed/city-page.test.tsx
    tests/feed/category-tabs.test.tsx
    tests/cities/city-search.test.ts
  </files>
  <action>
Create four test stub files. Each file contains ONLY test.todo() calls with descriptive names — zero src imports, zero describe-block imports.

tests/feed/feed-service.test.ts:
```
import { describe, test } from 'vitest'

describe('getFeedForCity', () => {
  test.todo('returns posts ordered by createdAt DESC')
  test.todo('excludes soft-deleted posts (deletedAt not null)')
  test.todo('excludes posts with status hidden or removed')
  test.todo('excludes past events (endsAt before NOW)')
  test.todo('filters by place category when category param provided')
  test.todo('returns events when category param is "event"')
  test.todo('returns all content types when category is "all" or omitted')
  test.todo('returns nextCursor when more posts exist beyond the limit')
  test.todo('returns nextCursor null on last page')
  test.todo('with cursor: returns next page without duplicating previous page items')
})

describe('getPostsForMap', () => {
  test.todo('returns only place posts (content_type = place)')
  test.todo('excludes posts with null lat or null lng')
  test.todo('excludes soft-deleted posts')
  test.todo('returns at most 200 rows')
})
```

tests/feed/city-page.test.tsx:
```
import { describe, test } from 'vitest'

describe('CityPage', () => {
  test.todo('renders post titles from feed in the page')
  test.todo('renders city name as heading')
  test.todo('shows not found for unknown city slug')
})
```

tests/feed/category-tabs.test.tsx:
```
import { describe, test } from 'vitest'

describe('CategoryFilterTabs', () => {
  test.todo('renders a link for each category including "All"')
  test.todo('All tab href is /cities/[slug] with no query string')
  test.todo('category tab href includes ?category=[value]')
  test.todo('active category tab has aria or class indicating active state')
})
```

tests/cities/city-search.test.ts:
```
import { describe, test } from 'vitest'

describe('searchCitiesByName', () => {
  test.todo('returns cities matching partial name (case-insensitive)')
  test.todo('returns empty array when no cities match')
  test.todo('limits results to 10')
})

describe('getCityBySlug', () => {
  test.todo('returns city for a valid slug')
  test.todo('returns null for an unknown slug')
})
```
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && npx vitest run tests/feed/feed-service.test.ts tests/feed/city-page.test.tsx tests/feed/category-tabs.test.tsx tests/cities/city-search.test.ts 2>&1 | tail -10</automated>
  </verify>
  <done>All four files exist. npx vitest run reports 0 failures (all tests are todo/skipped).</done>
</task>

<task type="auto">
  <name>Task 2: Create map test stub file</name>
  <files>
    tests/map/city-map.test.tsx
  </files>
  <action>
Create the map test stub. The map uses react-leaflet which requires a real DOM and calls window/document. The stub must include a comment documenting that the real test will use vi.mock('react-leaflet') — but the stub itself has zero imports and zero vi.mock calls (those come in Wave 2 when the component exists).

tests/map/city-map.test.tsx:
```
import { describe, test } from 'vitest'

// Note: real tests use vi.mock('react-leaflet') and vi.mock('leaflet')
// because Leaflet calls window/document at import time — jsdom required.
// These mocks are added in Wave 2 when CityMap component exists.

describe('CityMap', () => {
  test.todo('renders without throwing when passed an array of places')
  test.todo('renders a marker for each place')
  test.todo('renders empty map when places array is empty (Paris fallback center)')
})

describe('CityMapLoader', () => {
  test.todo('passes places from getPostsForMap to CityMap')
})
```

After creating all five files, run the full existing test suite to confirm zero regressions:
  </action>
  <verify>
    <automated>source ~/.nvm/nvm.sh && npx vitest run tests/map/city-map.test.tsx 2>&1 | tail -5 && npx vitest run 2>&1 | grep -E "Tests|passed|failed" | tail -5</automated>
  </verify>
  <done>tests/map/city-map.test.tsx exists. Full vitest suite (npx vitest run) is green — 0 failures. All five Phase 3 stub files are present.</done>
</task>

</tasks>

<verification>
After both tasks:
- npx vitest run reports 0 failures
- ls tests/feed/ shows feed-service.test.ts, city-page.test.tsx, category-tabs.test.tsx
- ls tests/cities/ shows city-search.test.ts
- ls tests/map/ shows city-map.test.tsx
- grep -r "import.*src\|from.*@/" tests/feed/ tests/cities/ tests/map/ returns nothing (zero src imports)
</verification>

<success_criteria>
Five test stub files exist. Full vitest suite is green. No src imports in any stub. Each stub file contains only test.todo() calls covering the behaviors listed in 03-VALIDATION.md Wave 0 Requirements.
</success_criteria>

<output>
After completion, create .planning/phases/03-city-feed-and-discovery/03-00-SUMMARY.md
</output>
