---
phase: 01-foundation
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.config.ts
  - tests/auth/register.test.ts
  - tests/auth/callback.test.ts
  - tests/auth/reset.test.ts
  - tests/middleware.test.ts
  - tests/roles/city-role.test.ts
  - tests/profile/profile-page.test.tsx
  - tests/db/schema.test.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running `npx vitest run` exits non-zero with 'no tests passed' (stubs exist but all tests are pending/skip)"
    - "All test files referenced by VALIDATION.md exist on disk"
    - "vitest.config.ts is present and valid — vitest can discover the test files"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Test runner configuration (jsdom environment, vite-tsconfig-paths, coverage)"
    - path: "tests/auth/register.test.ts"
      provides: "Stubs for AUTH-01, ROLE-01"
    - path: "tests/auth/callback.test.ts"
      provides: "Stubs for AUTH-02, AUTH-03 callback route"
    - path: "tests/auth/reset.test.ts"
      provides: "Stubs for AUTH-03 password reset action"
    - path: "tests/middleware.test.ts"
      provides: "Stubs for AUTH-04, ROLE-03"
    - path: "tests/roles/city-role.test.ts"
      provides: "Stubs for ROLE-02"
    - path: "tests/profile/profile-page.test.tsx"
      provides: "Stubs for ROLE-04"
    - path: "tests/db/schema.test.ts"
      provides: "Stubs for soft-delete and unique constraint checks"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/**/*.test.{ts,tsx}"
      via: "include glob"
      pattern: "include.*tests"
---

<objective>
Install the Vitest test framework and create failing test stubs for every behavior listed in VALIDATION.md — before any implementation exists. This satisfies the Nyquist rule: every subsequent task must have a runnable automated verify command.

Purpose: Tests must exist before code so executors can run RED checks immediately on task completion. Without this wave, all subsequent verify commands reference non-existent files.
Output: 8 test files + vitest.config.ts. All tests fail with "not implemented" or use `test.todo()`. The suite runs without errors.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test framework dependencies and create vitest.config.ts</name>
  <files>vitest.config.ts, package.json</files>
  <action>
The project does not exist yet as a Next.js app — this plan runs before 01-01 creates the scaffold. Therefore this task creates ONLY the vitest config and test stubs as standalone files. The actual `npm install` of vitest and testing-library is performed in plan 01-01 alongside the full scaffold install. This task writes the config file only.

Create `vitest.config.ts` at project root with this exact content:

```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import tsconfigPaths from 'vite-tsconfig-paths'

export default defineConfig({
  plugins: [react(), tsconfigPaths()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: [],
    include: ['tests/**/*.test.{ts,tsx}'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'lcov'],
      include: ['src/**/*.{ts,tsx}'],
    },
  },
})
```

NOTE: If the project scaffold (Next.js app) does not exist yet, this file will sit at root level and be incorporated when 01-01 runs `npx create-next-app`. If the scaffold already exists, place `vitest.config.ts` at the project root alongside `package.json`.
  </action>
  <verify>
    <automated>test -f vitest.config.ts && echo "vitest.config.ts exists"</automated>
  </verify>
  <done>vitest.config.ts file exists at project root with jsdom environment, vite-tsconfig-paths plugin, and tests/** include glob.</done>
</task>

<task type="auto">
  <name>Task 2: Create failing test stubs for all VALIDATION.md behaviors</name>
  <files>
    tests/auth/register.test.ts,
    tests/auth/callback.test.ts,
    tests/auth/reset.test.ts,
    tests/middleware.test.ts,
    tests/roles/city-role.test.ts,
    tests/profile/profile-page.test.tsx,
    tests/db/schema.test.ts
  </files>
  <action>
Create each test file with `test.todo()` stubs for the behaviors listed in VALIDATION.md. Each stub must reference the requirement ID in a comment. Do NOT import any src files (they don't exist yet) — stubs use `test.todo()` only.

**tests/auth/register.test.ts**
```typescript
import { describe, test } from 'vitest'

// AUTH-01: User can create an account with email and password
// ROLE-01: home_city_id is set during registration
describe('registerUser Server Action', () => {
  test.todo('AUTH-01: returns field errors when email is invalid')
  test.todo('AUTH-01: returns field errors when password is shorter than 8 characters')
  test.todo('AUTH-01: returns field errors when username contains invalid characters')
  test.todo('AUTH-01: returns field errors when username is shorter than 3 characters')
  test.todo('ROLE-01: accepts optional homeCityId UUID and includes it in validated output')
  test.todo('AUTH-01: returns _form error when Supabase signUp fails')
  test.todo('AUTH-01: redirects to /verify-email on success')
})
```

**tests/auth/callback.test.ts**
```typescript
import { describe, test } from 'vitest'

// AUTH-02: Email verification callback exchanges token_hash for session
// AUTH-03: Password reset callback redirects to /update-password
describe('GET /auth/callback', () => {
  test.todo('AUTH-02: redirects to / when token_hash type=email and verifyOtp succeeds')
  test.todo('AUTH-02: redirects to /auth/error when token_hash is missing')
  test.todo('AUTH-02: redirects to /auth/error when verifyOtp returns error')
  test.todo('AUTH-03: redirects to /update-password when token_hash type=recovery and verifyOtp succeeds')
  test.todo('AUTH-03: redirects to next param path when next is provided and type=email')
})
```

**tests/auth/reset.test.ts**
```typescript
import { describe, test } from 'vitest'

// AUTH-03: Password reset request and update
describe('resetPassword Server Action', () => {
  test.todo('AUTH-03: calls resetPasswordForEmail with correct redirectTo URL')
  test.todo('AUTH-03: returns error when email is not valid format')
  test.todo('AUTH-03: returns success indicator when Supabase call succeeds')
})

describe('updatePassword Server Action', () => {
  test.todo('AUTH-03: calls supabase.auth.updateUser with new password')
  test.todo('AUTH-03: returns error when password is shorter than 8 characters')
  test.todo('AUTH-03: redirects to login on success')
})
```

**tests/middleware.test.ts**
```typescript
import { describe, test } from 'vitest'

// AUTH-04: Session persists across browser refresh (middleware refreshes tokens)
// ROLE-03: requireAuth redirects unauthenticated users
describe('Next.js middleware (session refresh)', () => {
  test.todo('AUTH-04: calls supabase.auth.getUser() on every non-static request')
  test.todo('AUTH-04: sets updated session cookies on response')
  test.todo('ROLE-03: redirects unauthenticated user to /login for protected route')
  test.todo('ROLE-03: allows unauthenticated user to access /cities routes')
  test.todo('ROLE-03: allows unauthenticated user to access /login and /register routes')
  test.todo('ROLE-03: does not redirect authenticated user away from protected route')
})
```

**tests/roles/city-role.test.ts**
```typescript
import { describe, test } from 'vitest'

// ROLE-02: isUserLocalInCity returns correct value based on user_city_roles table
describe('isUserLocalInCity', () => {
  test.todo('ROLE-02: returns false when no user_city_roles row exists for (userId, cityId)')
  test.todo('ROLE-02: returns false when row exists but isLocal is false')
  test.todo('ROLE-02: returns true when row exists and isLocal is true')
  test.todo('ROLE-02: returns false when verifiedPostCount is 2 (below threshold)')
})
```

**tests/profile/profile-page.test.tsx**
```typescript
import { describe, test } from 'vitest'

// ROLE-04: Public profile page shows user's contributions and received ratings
describe('Profile page (/profile/[username])', () => {
  test.todo('ROLE-04: renders username in page heading')
  test.todo('ROLE-04: renders home city name when homeCityId is set')
  test.todo('ROLE-04: renders "0 contributions" when user has no posts')
  test.todo('ROLE-04: returns 404 when username does not exist in profiles table')
})
```

**tests/db/schema.test.ts**
```typescript
import { describe, test } from 'vitest'

// Schema: soft-delete and unique constraint validation
describe('Drizzle schema — soft-delete foundation', () => {
  test.todo('Schema: posts table has deletedAt timestamp column (nullable)')
  test.todo('Schema: user_city_roles table has unique index on (userId, cityId)')
  test.todo('Schema: posts table has status column with contentStatusEnum default active')
  test.todo('Schema: reports table has targetType and targetId columns')
  test.todo('Schema: isNull(posts.deletedAt) filter correctly excludes soft-deleted rows')
})
```
  </action>
  <verify>
    <automated>ls tests/auth/register.test.ts tests/auth/callback.test.ts tests/auth/reset.test.ts tests/middleware.test.ts tests/roles/city-role.test.ts tests/profile/profile-page.test.tsx tests/db/schema.test.ts && echo "all stubs exist"</automated>
  </verify>
  <done>All 7 test files exist. Each file contains only test.todo() stubs with requirement ID comments. No imports of src files. Files match the paths in VALIDATION.md exactly.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `ls vitest.config.ts` — config exists
2. `ls tests/auth/ tests/roles/ tests/profile/ tests/db/` — all directories and files present
3. All 8 Wave 0 items from VALIDATION.md are checked off
</verification>

<success_criteria>
- vitest.config.ts exists with jsdom environment and tests/** glob
- 7 test stub files exist matching VALIDATION.md Wave 0 requirements exactly
- Every behavior from VALIDATION.md Per-Task Verification Map has a corresponding test.todo() stub
- No src imports in any stub file (src doesn't exist yet)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md` following the summary template.
</output>
