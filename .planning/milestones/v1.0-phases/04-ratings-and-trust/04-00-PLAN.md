---
phase: 04-ratings-and-trust
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/ratings/create-review.test.ts
  - tests/ratings/report.test.ts
  - tests/ratings/rating-badge.test.tsx
autonomous: true
requirements:
  - RATE-01
  - RATE-02
  - RATE-03
  - RATE-04

must_haves:
  truths:
    - "Full vitest suite stays green after stubs are added (no false positives)"
    - "Each stub file contains only test.todo() calls with no src imports"
    - "Every behavior listed in the validation map has a named todo stub"
  artifacts:
    - path: "tests/ratings/create-review.test.ts"
      provides: "Stub tests for RATE-01, RATE-02, RATE-04 (createReview, validation, write-through)"
    - path: "tests/ratings/report.test.ts"
      provides: "Stub tests for RATE-03 (reportContent action, flagCount increment)"
    - path: "tests/ratings/rating-badge.test.tsx"
      provides: "Stub tests for RATE-04 (RatingBadge display, feed integration)"
  key_links:
    - from: "tests/ratings/"
      to: "npx vitest run"
      via: "vitest auto-discovery"
      pattern: "tests/ratings/.*\\.test\\.(ts|tsx)"
---

<objective>
Create Wave 0 test stub files for Phase 4 using test.todo() with zero src imports. These establish the Nyquist baseline — the full suite must stay green before any implementation begins.

Purpose: Prevent false positives. Every named behavior has a failing-safe placeholder. Implementation plans (04-01 through 04-03) replace these todos with real assertions.
Output: Three test files with named test.todo() stubs covering RATE-01 through RATE-04.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ratings-and-trust/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stub files</name>
  <files>
    tests/ratings/create-review.test.ts,
    tests/ratings/report.test.ts,
    tests/ratings/rating-badge.test.tsx
  </files>
  <action>
Create the three stub files below. Every file must satisfy these invariants:
- Zero src imports (no @/actions/*, @/lib/*, @/components/*)
- Only test.todo() calls — no describe blocks with real assertions
- No vi.mock() calls (nothing to mock yet)
- Each todo name matches the behavior it will test (names are the contract)

FILE: tests/ratings/create-review.test.ts
```typescript
import { test } from 'vitest'

// RATE-01: Star rating creation
test.todo('createReview inserts a review row with stars 1')
test.todo('createReview inserts a review row with stars 5')
test.todo('createReview rejects stars = 0 (below minimum)')
test.todo('createReview rejects stars = 6 (above maximum)')
test.todo('createReview blocks duplicate review from same user on same post')
test.todo('createReview returns error for unauthenticated caller')

// RATE-02: Optional written review body
test.todo('createReview accepts null body (review without text)')
test.todo('createReview accepts body up to 2000 chars')
test.todo('createReview rejects body longer than 2000 chars')

// RATE-04: rating_summary write-through in same transaction
test.todo('createReview upserts rating_summary with recalculated avg and count')
test.todo('second review from different user updates avgRating in rating_summary')
test.todo('deleteReview recalculates rating_summary after removal')
```

FILE: tests/ratings/report.test.ts
```typescript
import { test } from 'vitest'

// RATE-03: Report/flag content
test.todo('reportContent inserts a row into the reports table')
test.todo('reportContent returns error for unauthenticated caller')
test.todo('reportContent increments flagCount on the target post')
test.todo('reportContent increments flagCount on the target review')
test.todo('reportContent returns early if target status is not active')
test.todo('ReportButton renders on place detail page')
test.todo('ReportButton renders on event detail page')
test.todo('ReportButton renders on a review item')
```

FILE: tests/ratings/rating-badge.test.tsx
```typescript
import { test } from 'vitest'

// RATE-04: RatingBadge display component
test.todo('RatingBadge renders avg rating formatted to 1 decimal place')
test.todo('RatingBadge renders review count')
test.todo('RatingBadge renders nothing when reviewCount is 0 or null')
test.todo('RatingBadge has accessible aria-label with avg and count')
test.todo('StarRating renders correct number of filled stars')
test.todo('StarRating renders correct number of empty stars')
test.todo('FeedCard renders RatingBadge when avgRating and reviewCount are present')
test.todo('FeedCard renders nothing for rating when reviewCount is null')
```
  </action>
  <verify>
    <automated>npx vitest run tests/ratings/</automated>
  </verify>
  <done>All three files exist. `npx vitest run tests/ratings/` exits 0 with all tests shown as "todo". Full suite `npx vitest run` also exits 0.</done>
</task>

</tasks>

<verification>
`npx vitest run tests/ratings/` — all stubs pass as todo, exit 0.
`npx vitest run` — full suite still green (119+ tests passing).
</verification>

<success_criteria>
- tests/ratings/ directory contains exactly three files
- Zero src imports in any stub file
- All test names match the validation map behaviors from 04-VALIDATION.md
- Full suite green
</success_criteria>

<output>
After completion, create `.planning/phases/04-ratings-and-trust/04-00-SUMMARY.md`
</output>
