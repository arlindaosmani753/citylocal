---
phase: 04-ratings-and-trust
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/lib/db/queries/feed.ts
  - src/components/feed/FeedCard.tsx
  - tests/feed/feed-service.test.ts
  - tests/ratings/rating-badge.test.tsx
autonomous: false
requirements:
  - RATE-04

must_haves:
  truths:
    - "getFeedForCity returns avgRating and reviewCount on every FeedPost item"
    - "FeedCard renders RatingBadge when avgRating and reviewCount are present"
    - "FeedCard renders nothing for ratings when reviewCount is null"
    - "Existing feed tests continue to pass (no regression)"
    - "User can visually confirm rating badges on feed cards in browser"
  artifacts:
    - path: "src/lib/db/queries/feed.ts"
      provides: "FeedPost type extended with avgRating and reviewCount; getFeedForCity leftJoins ratingSummary"
      contains: "ratingSummary"
    - path: "src/components/feed/FeedCard.tsx"
      provides: "Renders RatingBadge using FeedPost.avgRating and FeedPost.reviewCount"
  key_links:
    - from: "src/lib/db/queries/feed.ts"
      to: "src/lib/db/schema.ts ratingSummary"
      via: "leftJoin(ratingSummary, eq(ratingSummary.postId, posts.id))"
      pattern: "leftJoin.*ratingSummary"
    - from: "src/components/feed/FeedCard.tsx"
      to: "src/components/ratings/RatingBadge.tsx"
      via: "RatingBadge component with post.avgRating and post.reviewCount"
      pattern: "RatingBadge"
---

<objective>
Extend getFeedForCity to join rating_summary and surface avgRating/reviewCount on FeedPost. Update FeedCard to render RatingBadge. This closes RATE-04 by making ratings visible everywhere content appears.

Purpose: Closes the trust loop — tourists see aggregate ratings on feed cards before clicking through to detail pages.
Output: Feed query extended, FeedCard updated, tests updated, human checkpoint confirms visual appearance.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-ratings-and-trust/04-RESEARCH.md
@.planning/phases/04-ratings-and-trust/04-02-SUMMARY.md

@src/lib/db/queries/feed.ts
@src/components/feed/FeedCard.tsx
@tests/feed/feed-service.test.ts
@tests/ratings/rating-badge.test.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

Current FeedPost type (from src/lib/db/queries/feed.ts):
```typescript
export type FeedPost = {
  id: string
  title: string
  contentType: 'place' | 'event'
  category: string | null
  body: string | null
  lat: string | null
  lng: string | null
  authorUsername: string | null
  createdAt: Date
  startsAt: Date | null
  endsAt: Date | null
  firstImagePath: string | null
  // ADD THESE TWO:
  // avgRating: string | null    — decimal returns as string from Drizzle
  // reviewCount: number | null  — null when no rating_summary row exists (leftJoin)
}
```

RatingBadge contract (from src/components/ratings/RatingBadge.tsx, created in 04-02):
```typescript
export function RatingBadge({ avgRating, reviewCount }: {
  avgRating: string | null
  reviewCount: number | null
}): JSX.Element | null
// Returns null when reviewCount is falsy
```

Pitfall (from 04-RESEARCH.md): avgRating is string | null — decimal columns return string in Drizzle.
Pitfall: DO NOT run AVG() in getFeedForCity — always read from ratingSummary via leftJoin.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend FeedPost type, add ratingSummary join, update FeedCard</name>
  <files>
    src/lib/db/queries/feed.ts,
    src/components/feed/FeedCard.tsx,
    tests/feed/feed-service.test.ts,
    tests/ratings/rating-badge.test.tsx
  </files>
  <behavior>
    - FeedPost type gains two fields: avgRating (string | null), reviewCount (number | null)
    - getFeedForCity select object includes ratingSummary.avgRating and ratingSummary.reviewCount
    - getFeedForCity from/join chain adds: .leftJoin(ratingSummary, eq(ratingSummary.postId, posts.id))
    - ratingSummary imported from '@/lib/db/schema' in feed.ts
    - FeedCard renders RatingBadge when post.avgRating and post.reviewCount are present (passes both props)
    - FeedCard renders nothing for ratings when post.reviewCount is null or 0
    - Existing feed-service.test.ts tests still pass (makeFeedPost helper updated to include avgRating: null, reviewCount: null defaults)
    - FeedCard tests in rating-badge.test.tsx now pass (FeedPost type now has the fields)
  </behavior>
  <action>
Update src/lib/db/queries/feed.ts:
1. Add `ratingSummary` to imports from '@/lib/db/schema'
2. Extend `FeedPost` type with `avgRating: string | null` and `reviewCount: number | null`
3. Add `avgRating: ratingSummary.avgRating` and `reviewCount: ratingSummary.reviewCount` to the select object inside getFeedForCity (inside the `db.select({...})` call)
4. Add `.leftJoin(ratingSummary, eq(ratingSummary.postId, posts.id))` after the existing postImages leftJoin in the query chain
5. Do NOT change getPostsForMap — it does not need ratings

Update src/components/feed/FeedCard.tsx:
1. Add import: `import { RatingBadge } from '@/components/ratings/RatingBadge'`
2. Inside the returned `<article>`, add `<RatingBadge avgRating={post.avgRating} reviewCount={post.reviewCount} />` after the author/time line
3. FeedCard already receives `post: FeedPost` — TypeScript will enforce the new fields exist

Update tests/feed/feed-service.test.ts:
- In `makeFeedPost` helper, add `avgRating: null` and `reviewCount: null` to the default object so existing tests don't break when FeedPost type gains the new fields

Update tests/ratings/rating-badge.test.tsx:
- Replace FeedCard test.todo() stubs with real assertions:
  - Test: render FeedCard with post having avgRating='4.2' reviewCount=17 — expect screen to have text matching '4.2' and '17'
  - Test: render FeedCard with post having reviewCount=null — expect no rating badge in output

Run `npx vitest run tests/feed/ tests/ratings/` after implementation — must be GREEN.
  </action>
  <verify>
    <automated>npx vitest run tests/feed/ tests/ratings/</automated>
  </verify>
  <done>All feed-service and rating-badge tests pass. FeedPost type has avgRating and reviewCount fields. getFeedForCity leftJoins ratingSummary. FeedCard renders RatingBadge. TypeScript compiles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — end-to-end ratings flow</name>
  <action>Run npm run dev and verify the end-to-end ratings flow as described in how-to-verify below.</action>
  <verify>Human confirms all steps in how-to-verify pass.</verify>
  <done>User types "approved" to confirm ratings are visible and functional end-to-end.</done>
  <what-built>
Full Phase 4 ratings system: authenticated users can leave 1–5 star ratings with optional review text on places and events. Average rating and review count appear on feed cards and detail pages. Any user can report/flag content.
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Visit http://localhost:3000/cities/paris-france — confirm feed cards have a star/rating badge on any item that has been reviewed (will be blank initially — that is correct)
3. Navigate to a place detail page (click a place card) — confirm the page shows: a rating badge area (blank = no reviews yet), a "Leave a Review" form with 5 clickable stars and optional text, a "Report" button near the top
4. Log in as a test user, click stars 1–5 in the review form, verify selection highlights change, submit a review — confirm success message appears
5. Refresh the page — confirm the review you submitted appears in the review list with the correct star count
6. Return to the city feed at /cities/paris-france — confirm the reviewed place's card now shows the rating badge with the avg and count
7. Navigate to an event detail page — confirm the same review/report UI is present
8. Test the Report button: click Report on any post or review, select a reason, confirm — verify the success message appears
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
`npx vitest run tests/feed/ tests/ratings/` — all green.
`npx vitest run` — full suite green (target: 130+ tests).
`npx tsc --noEmit` — zero errors.
Human checkpoint: ratings visible on feed cards and detail pages; review submission works; report button works.
</verification>

<success_criteria>
- FeedPost type has avgRating and reviewCount fields
- getFeedForCity joins ratingSummary and returns rating fields on every post
- FeedCard renders RatingBadge
- All feed and rating tests pass
- Human confirms: ratings appear on feed cards, review form works end-to-end, report button works
- RATE-01, RATE-02, RATE-03, RATE-04 all addressed
</success_criteria>

<output>
After completion, create `.planning/phases/04-ratings-and-trust/04-03-SUMMARY.md`
</output>
